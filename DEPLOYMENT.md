# 🚀 Руководство по развертыванию

## Быстрый старт (одна команда)

```bash
make deploy
```

Эта команда выполнит:
1. Сборку Docker образов
2. Запуск контейнеров (PostgreSQL + API)
3. Применение миграций к БД
4. Создание пользователей (admin@admin.admin и test@test.test)

---

## Предварительные требования

- Docker Engine 20.10+
- Docker Compose 2.0+
- Make (для Windows: установите через Chocolatey или WSL)

### Установка Make на Windows

**Через Chocolatey:**
```powershell
choco install make
```

**Через WSL:**
```bash
wsl --install
# Затем в WSL:
sudo apt-get update
sudo apt-get install make
```

---

## Пошаговое развертывание

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd cl-backend
```

### 2. Настройка переменных окружения (опционально)

```bash
# Создать .env файл из примера
make env-example

# Отредактировать .env (по желанию)
nano .env
```

**Доступные переменные (.env.example):**
```env
POSTGRES_DB=cl_backend_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=210176
POSTGRES_PORT=54321
API_PORT=5000
```

### 3. Полное развертывание

```bash
make deploy
```

**Или пошагово:**

```bash
# Шаг 1: Сборка образов
make build

# Шаг 2: Запуск контейнеров
make up

# Шаг 3: Применение миграций
make migrate

# Шаг 4: Создание пользователей
make init-users
```

---

## 📋 Доступные команды Make

### Основные команды

| Команда | Описание |
|---------|----------|
| `make deploy` | Полное развертывание (build + up + migrate + init-users) |
| `make help` | Показать все доступные команды |
| `make build` | Собрать Docker образы |
| `make up` | Запустить контейнеры в фоне |
| `make down` | Остановить и удалить контейнеры |
| `make restart` | Перезапустить контейнеры |
| `make logs` | Показать логи всех контейнеров |

### База данных

| Команда | Описание |
|---------|----------|
| `make migrate` | Применить миграции |
| `make init-db` | Пересоздать БД с нуля |
| `make db-shell` | Подключиться к БД через psql |
| `make db-backup` | Создать backup БД |
| `make db-restore FILE=backup.sql` | Восстановить БД |

### Пользователи

| Команда | Описание |
|---------|----------|
| `make init-users` | Создать тестовых пользователей |

**Создаваемые пользователи:**
- **Администратор:** admin@admin.admin / admin@admin.admin
- **Пользователь:** test@test.test / test@test.test

### Разработка

| Команда | Описание |
|---------|----------|
| `make dev` | Запуск с логами (foreground) |
| `make rebuild` | Пересобрать без кеша |
| `make shell-api` | Подключиться к контейнеру API |
| `make shell-db` | Подключиться к контейнеру БД |

### Очистка

| Команда | Описание |
|---------|----------|
| `make clean` | Удалить всё (контейнеры, образы, volumes) |
| `make clean-volumes` | Удалить volumes (БД будет потеряна!) |

### Тестирование

| Команда | Описание |
|---------|----------|
| `make test-api` | Проверить доступность API |
| `make test-db` | Проверить подключение к БД |
| `make test-all` | Проверить все сервисы |

### Информация

| Команда | Описание |
|---------|----------|
| `make info` | Показать информацию о проекте |
| `make ps` | Показать статус контейнеров |

---

## 🌐 Доступ к сервисам

После развертывания сервисы будут доступны по следующим адресам:

| Сервис | URL | Порт |
|--------|-----|------|
| **API** | http://localhost:5000 | 5000 |
| **Swagger UI** | http://localhost:5000/swagger | 5000 |
| **PostgreSQL** | localhost:54321 | 54321 |

### Подключение к БД

```bash
# Через Docker
make db-shell

# Напрямую (если установлен psql)
psql -h localhost -p 54321 -U postgres -d cl_backend_db
```

**Пароль:** 210176

---

## 🔐 Тестовые пользователи

После выполнения `make init-users` будут созданы:

### Администратор
- **Логин:** admin@admin.admin
- **Пароль:** admin@admin.admin
- **Роль:** admin

### Обычный пользователь
- **Логин:** test@test.test
- **Пароль:** test@test.test
- **Роль:** user

---

## 📝 Примеры использования API

### Регистрация пользователя

```bash
curl -X POST http://localhost:5000/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "newuser@example.com",
    "password": "password123"
  }'
```

### Вход в систему

```bash
curl -X POST http://localhost:5000/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin@admin.admin",
    "password": "admin@admin.admin"
  }'
```

**Ответ:**
```json
{
  "success": true,
  "message": "Login successful!",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "id": 1,
    "login": "admin@admin.admin",
    "role": "admin"
  }
}
```

### Использование токена

```bash
TOKEN="your_jwt_token_here"

curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:5000/api/admin/reports/dashboard
```

---

## 🔧 Устранение неполадок

### API не запускается

```bash
# Проверить логи
make logs-api

# Перезапустить
make restart
```

### БД не подключается

```bash
# Проверить статус БД
make test-db

# Проверить логи БД
make logs-db

# Пересоздать БД
make init-db
```

### Контейнеры не останавливаются

```bash
# Принудительная остановка
docker compose kill

# Очистка
make clean
```

### Порт уже занят

Измените порты в `.env`:

```env
API_PORT=5001
POSTGRES_PORT=54322
```

Затем перезапустите:

```bash
make down
make up
```

---

## 🐳 Docker команды (без Make)

Если Make недоступен, используйте Docker Compose напрямую:

```bash
# Запуск
docker compose up -d

# Остановка
docker compose down

# Логи
docker compose logs -f

# Перезапуск
docker compose restart

# Статус
docker compose ps
```

---

## 📦 Структура проекта

```
cl-backend/
├── cl-backend/                 # Исходный код API
│   ├── Controllers/           # Контроллеры
│   ├── Models/                # Модели данных
│   ├── DTO/                   # Data Transfer Objects
│   ├── Services/              # Сервисы (экспорт и др.)
│   ├── DbContexts/            # Entity Framework контекст
│   └── Migrations/            # Миграции БД
├── scripts/                   # Вспомогательные скрипты
│   ├── init-users.sh         # Создание пользователей
│   └── init-users.sql        # SQL скрипт (альтернатива)
├── uploads/                   # Загруженные файлы
├── Dockerfile                 # Образ для API
├── docker-compose.yml         # Конфигурация Docker Compose
├── Makefile                   # Команды управления проектом
├── .env.example               # Пример переменных окружения
└── DEPLOYMENT.md              # Эта инструкция
```

---

## 🚀 Развертывание на удаленном сервере

### 1. Подключитесь к серверу

```bash
ssh user@your-server.com
```

### 2. Установите Docker и Docker Compose

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Проверка
docker --version
docker-compose --version
```

### 3. Клонируйте репозиторий

```bash
git clone <repository-url>
cd cl-backend
```

### 4. Настройте .env (опционально)

```bash
cp .env.example .env
nano .env
```

### 5. Разверните проект

```bash
make deploy
```

### 6. Откройте порты в firewall

```bash
# Ubuntu/Debian
sudo ufw allow 5000/tcp
sudo ufw allow 54321/tcp

# CentOS/RHEL
sudo firewall-cmd --permanent --add-port=5000/tcp
sudo firewall-cmd --permanent --add-port=54321/tcp
sudo firewall-cmd --reload
```

### 7. Настройте Nginx (опционально)

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

---

## 📊 Мониторинг

### Проверка здоровья сервисов

```bash
# Статус контейнеров
make ps

# Проверка API
make test-api

# Проверка БД
make test-db

# Все проверки
make test-all
```

### Просмотр логов

```bash
# Все логи
make logs

# Только API
make logs-api

# Только БД
make logs-db
```

---

## 🔄 Обновление проекта

```bash
# Получить последние изменения
git pull

# Пересобрать и перезапустить
make rebuild

# Применить новые миграции
make migrate
```

---

## 💾 Резервное копирование

### Создание backup

```bash
make db-backup
```

Файл будет сохранен как `backup_YYYYMMDD_HHMMSS.sql`

### Восстановление из backup

```bash
make db-restore FILE=backup_20241115_143022.sql
```

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `make logs`
2. Проверьте статус: `make ps`
3. Проверьте доступность: `make test-all`
4. Создайте issue в репозитории

---

## 📄 Лицензия

MIT
