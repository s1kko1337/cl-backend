# 📦 Docker Setup - Сводка

## ✅ Созданные файлы для развертывания

### Основные файлы Docker

1. **[Dockerfile](Dockerfile)** - Мультистейдж сборка .NET приложения
   - Stage 1: Build (сборка)
   - Stage 2: Publish (публикация)
   - Stage 3: Runtime (финальный образ)

2. **[docker-compose.yml](docker-compose.yml)** - Оркестрация сервисов
   - PostgreSQL 16 Alpine
   - .NET 9.0 API
   - Настроенные volumes и networks
   - Health checks

3. **[.dockerignore](.dockerignore)** - Исключения для Docker build

### Конфигурация

4. **[.env](.env)** - Переменные окружения (рабочий файл)
   ```env
   POSTGRES_DB=cl_backend_db
   POSTGRES_USER=postgres
   POSTGRES_PASSWORD=210176
   POSTGRES_PORT=54321
   API_PORT=5000
   ```

5. **[.env.example](.env.example)** - Пример конфигурации (для VCS)

### Автоматизация

6. **[Makefile](Makefile)** - Команды для управления проектом
   - 30+ команд для всех операций
   - Цветной вывод
   - Понятные алиасы

7. **[scripts/init-users.sh](scripts/init-users.sh)** - Скрипт создания пользователей
   - Автоматическое ожидание готовности API
   - Регистрация через API
   - Обновление ролей через БД

### Документация

8. **[README.md](README.md)** - Главная страница проекта
   - Обзор возможностей
   - Быстрый старт
   - Список endpoints

9. **[DEPLOYMENT.md](DEPLOYMENT.md)** - Полное руководство по развертыванию
   - Детальные инструкции
   - Все команды Make
   - Troubleshooting
   - Развертывание на удаленном сервере

10. **[QUICKSTART.md](QUICKSTART.md)** - Шпаргалка для быстрого старта

11. **[API_DOCUMENTATION_ADMIN.md](API_DOCUMENTATION_ADMIN.md)** - API документация
    - Все 18 административных endpoints
    - Примеры для Kotlin/Android
    - Retrofit интерфейсы

12. **[ADMIN_ENDPOINTS_QUICK_REFERENCE.md](ADMIN_ENDPOINTS_QUICK_REFERENCE.md)** - Краткий справочник

---

## 🚀 Как использовать

### 1. Первый запуск (одна команда)

```bash
make deploy
```

Эта команда выполнит:
- ✅ Сборку Docker образов
- ✅ Запуск PostgreSQL
- ✅ Запуск API
- ✅ Применение миграций
- ✅ Создание пользователей:
  - admin@admin.admin / admin@admin.admin (роль: admin)
  - test@test.test / test@test.test (роль: user)

### 2. Доступ к сервисам

После запуска доступны:

| Сервис | URL | Порт |
|--------|-----|------|
| API | http://localhost:5000 | 5000 |
| Swagger UI | http://localhost:5000/swagger | 5000 |
| PostgreSQL | localhost:54321 | 54321 |

### 3. Проверка работы

```bash
# Статус контейнеров
make ps

# Логи
make logs

# Проверка API
make test-api

# Проверка БД
make test-db
```

---

## 📊 Архитектура развертывания

```
┌─────────────────────────────────────┐
│         Docker Compose              │
│                                     │
│  ┌──────────────┐  ┌─────────────┐ │
│  │              │  │             │ │
│  │  PostgreSQL  │◄─┤   .NET API  │ │
│  │   (16-alpine)│  │   (9.0)     │ │
│  │              │  │             │ │
│  │  Port: 54321 │  │  Port: 5000 │ │
│  │              │  │             │ │
│  └──────┬───────┘  └──────┬──────┘ │
│         │                 │         │
│         │                 │         │
│  ┌──────▼─────────────────▼──────┐ │
│  │      backend-network          │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │   Volumes                     │ │
│  │   - pgsql (PostgreSQL data)   │ │
│  │   - ./uploads (API files)     │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

## 🔧 Основные команды

### Управление контейнерами

```bash
# Запуск
make up              # Запустить все сервисы
make start           # Запустить остановленные контейнеры
make dev             # Запустить с логами (foreground)

# Остановка
make stop            # Остановить (без удаления)
make down            # Остановить и удалить контейнеры

# Перезапуск
make restart         # Перезапустить все сервисы
make rebuild         # Пересобрать и перезапустить
```

### База данных

```bash
# Миграции
make migrate         # Применить миграции

# Пересоздание
make init-db         # Полностью пересоздать БД

# Подключение
make db-shell        # Подключиться к psql

# Backup
make db-backup       # Создать backup
make db-restore FILE=backup.sql  # Восстановить
```

### Пользователи

```bash
# Создание тестовых пользователей
make init-users
```

### Логи и отладка

```bash
# Логи
make logs            # Все логи
make logs-api        # Только API
make logs-db         # Только PostgreSQL

# Подключение к контейнерам
make shell-api       # Bash в контейнере API
make shell-db        # Bash в контейнере БД
```

### Очистка

```bash
make clean           # Удалить всё (контейнеры, образы, volumes)
make clean-volumes   # Удалить только volumes
make clean-logs      # Очистить Docker logs
```

---

## 🌐 Развертывание на сервере

### Минимальные требования

- Docker 20.10+
- Docker Compose 2.0+
- 2GB RAM
- 10GB disk space

### Быстрое развертывание

```bash
# 1. Подключение к серверу
ssh user@your-server.com

# 2. Клонирование
git clone <repository-url>
cd cl-backend

# 3. Запуск
make deploy

# 4. Открытие портов (опционально)
sudo ufw allow 5000/tcp
```

### С Nginx reverse proxy

```nginx
server {
    listen 80;
    server_name api.yourdomain.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

---

## 📝 Переменные окружения

### Основные переменные (.env)

```env
# База данных
POSTGRES_DB=cl_backend_db          # Имя БД
POSTGRES_USER=postgres              # Пользователь БД
POSTGRES_PASSWORD=210176            # Пароль БД
POSTGRES_PORT=54321                 # Внешний порт PostgreSQL

# API
API_PORT=5000                       # Внешний порт API

# Приложение
ASPNETCORE_ENVIRONMENT=Production   # Development, Staging, Production
```

### Для скрипта init-users.sh

```bash
API_URL=http://localhost:5000       # URL API
POSTGRES_CONTAINER=cl-backend-postgres  # Имя контейнера БД
```

---

## 🔐 Безопасность

### Рекомендации для продакшена

1. **Измените пароли:**
   ```env
   POSTGRES_PASSWORD=your_secure_password_here
   ```

2. **Используйте secrets:**
   ```bash
   # Docker secrets вместо .env
   docker secret create db_password password.txt
   ```

3. **Настройте HTTPS:**
   - Используйте Let's Encrypt с Nginx
   - Или настройте HTTPS в ASP.NET Core

4. **Ограничьте доступ:**
   ```bash
   # Не открывайте PostgreSQL порт наружу
   # В docker-compose.yml закомментируйте:
   # ports:
   #   - "54321:5432"
   ```

---

## 🧪 Тестирование

### Автоматические тесты

```bash
# Проверка всех сервисов
make test-all

# Отдельно
make test-api
make test-db
```

### Ручное тестирование

```bash
# Логин
curl -X POST http://localhost:5000/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin@admin.admin","password":"admin@admin.admin"}'

# Получение токена и использование
TOKEN="<ваш_токен>"
curl -H "Authorization: Bearer $TOKEN" \
  http://localhost:5000/api/admin/reports/dashboard
```

---

## 📦 Volumes и данные

### Где хранятся данные

- **PostgreSQL:** Docker volume `pgsql`
- **Uploads:** `./uploads` (bind mount)

### Backup и восстановление

```bash
# Backup PostgreSQL
make db-backup
# Создаст файл: backup_YYYYMMDD_HHMMSS.sql

# Восстановление
make db-restore FILE=backup_20241115_143022.sql

# Backup uploads
tar -czf uploads_backup.tar.gz uploads/

# Восстановление uploads
tar -xzf uploads_backup.tar.gz
```

---

## 🚨 Troubleshooting

### API не запускается

```bash
# Проверить логи
make logs-api

# Проверить, применены ли миграции
make migrate

# Пересобрать
make rebuild
```

### БД не подключается

```bash
# Проверить статус
make test-db

# Проверить логи
make logs-db

# Пересоздать
make init-db
```

### Порт занят

```bash
# Изменить в .env
API_PORT=5001
POSTGRES_PORT=54322

# Перезапустить
make down && make up
```

### Нет прав на выполнение скриптов

```bash
chmod +x scripts/init-users.sh
```

---

## 📚 Дополнительные ресурсы

- [Полная документация](DEPLOYMENT.md)
- [API документация](API_DOCUMENTATION_ADMIN.md)
- [Быстрый старт](QUICKSTART.md)
- [Docker Documentation](https://docs.docker.com/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [.NET Documentation](https://docs.microsoft.com/en-us/dotnet/)

---

## ✨ Что дальше?

После успешного развертывания:

1. ✅ API доступен на http://localhost:5000
2. ✅ Swagger UI на http://localhost:5000/swagger
3. ✅ Два тестовых пользователя созданы
4. ✅ База данных инициализирована

Можно начинать разработку фронтенда или тестирование API!
